---
grafana_agent_default_config:
  metrics:
    wal_directory: /var/lib/grafana-agent
  integrations:
    agent:
      enabled: true
    node_exporter:
      enabled: true
      include_exporter_metrics: true
